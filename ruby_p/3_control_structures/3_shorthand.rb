#!/usr/bin/env ruby

# Ternary Operator

=begin
  boolean ? result1 : result2
  puts count == 1 ? "result" : "result2"
=end

=begin
  x = y || z
  # same as 

  if Y    #y is true or has value
    x = y #Then x = y
  else
    x = z # else x = z
  end
=end

=begin
  x ||= y # if x has a value then ok x = x
          # if x doesn't have a value or is false then x = y
=end

